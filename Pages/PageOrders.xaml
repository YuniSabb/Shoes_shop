<Page x:Class="Shoes_shop.Pages.PageOrders"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Shoes_shop.Pages"
      mc:Ignorable="d"
      d:DesignHeight="650" d:DesignWidth="1000"
      Title="PageOrders"
      Background="#F5F5F5">

    <Page.Resources>
        <Style x:Key="TopButtonStyle" TargetType="Button">
            <Setter Property="FontFamily" Value="Times New Roman"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="14,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#00FA9A"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DeleteButtonStyle" TargetType="Button">
            <Setter Property="FontFamily" Value="Times New Roman"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Width" Value="82"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#00FA9A"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListView">
            <Setter Property="FontFamily" Value="Times New Roman"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style TargetType="GridViewColumnHeader">
            <Setter Property="FontFamily" Value="Times New Roman"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="#7FFF00"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="8"/>
        </Style>

        <Style TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="Margin" Value="0,0,0,2"/>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="55"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#7FFF00"
                CornerRadius="12"
                Padding="18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="170"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="Назад"
                        Width="110"
                        Style="{StaticResource TopButtonStyle}"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Click="BtnBack_Click"/>

                <TextBlock Grid.Column="1"
                           Text="Заказы"
                           FontFamily="Times New Roman"
                           FontSize="26"
                           FontWeight="Bold"
                           Foreground="Black"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"/>

                <Button x:Name="BtnAddOrder"
                        Grid.Column="2"
                        Content="Добавить"
                        Width="130"
                        Style="{StaticResource TopButtonStyle}"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Click="BtnAddOrder_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="1"
                Margin="0,15,0,15"
                Background="White"
                BorderBrush="#7FFF00"
                BorderThickness="2"
                CornerRadius="12"
                Padding="14">
            <TextBlock Text="Список оформленных заказов"
                       FontFamily="Times New Roman"
                       FontSize="17"
                       FontWeight="Bold"
                       Foreground="Black"
                       VerticalAlignment="Center"/>
        </Border>

        <Border Grid.Row="2"
                Background="White"
                BorderBrush="#7FFF00"
                BorderThickness="2"
                CornerRadius="12"
                Padding="12">

            <ListView x:Name="LvOrders"
                      SelectionChanged="LvOrders_SelectionChanged"
                      HorizontalContentAlignment="Stretch">

                <ListView.View>
                    <GridView>

                        <GridViewColumn Header="№" Width="60">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding OrderNumber}"
                                               TextAlignment="Center"
                                               VerticalAlignment="Center"
                                               Margin="4"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Дата заказа" Width="120">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding OrderDate}"
                                               TextAlignment="Center"
                                               VerticalAlignment="Center"
                                               Margin="4"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Дата выдачи" Width="120">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding DeliveryDate}"
                                               TextAlignment="Center"
                                               VerticalAlignment="Center"
                                               Margin="4"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Пункт выдачи" Width="250">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock TextWrapping="Wrap"
                                               VerticalAlignment="Center"
                                               Margin="4">
                                        <Run Text="{Binding PickupPoints.PostalCode}"/>
                                        <Run Text=", г. "/>
                                        <Run Text="{Binding PickupPoints.City}"/>
                                        <Run Text=", ул. "/>
                                        <Run Text="{Binding PickupPoints.Street}"/>
                                        <Run Text=", д. "/>
                                        <Run Text="{Binding PickupPoints.House}"/>
                                    </TextBlock>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Клиент" Width="190">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock VerticalAlignment="Center"
                                               Margin="4"
                                               TextWrapping="Wrap">
                                        <Run Text="{Binding Users.LastName}"/>
                                        <Run Text=" "/>
                                        <Run Text="{Binding Users.FirstName}"/>
                                        <Run Text=" "/>
                                        <Run Text="{Binding Users.MiddleName}"/>
                                    </TextBlock>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Статус" Width="110">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding OrderStatuses.StatusName}"
                                               TextAlignment="Center"
                                               VerticalAlignment="Center"
                                               Margin="4"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Код" Width="80">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding PickupCode}"
                                               TextAlignment="Center"
                                               VerticalAlignment="Center"
                                               Margin="4"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn x:Name="ColDeleteOrder" Header="Действие" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Удалить"
                                            Style="{StaticResource DeleteButtonStyle}"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Click="BtnDeleteOrder_Click"
                                            Tag="{Binding OrderNumber}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    </Grid>
</Page>